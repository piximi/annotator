import React from "react";
import { Category } from "../../../../../../types/Category";

type AnnotationProps = {
  category: Category;
  src: string;
};

export const Annotation = ({ category, src }: AnnotationProps) => {
  return <React.Fragment />;
};
